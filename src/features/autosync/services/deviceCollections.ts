export async function readDeviceCollections(): Promise<
  Array<{
    localId: string;
    name: string;
    assetCount: number;
    videoCount: number;
    sizeBytes: number;
  }>
> {
  return [];
}
