import { Pressable, StyleSheet, Text, View } from 'react-native';
import { theme, textStyles } from '../../theme/tokens';
export function SegmentedControl<T extends string>({ value, options, onChange, label }: { value: T; options: readonly { value: T; label: string }[]; onChange: (value: T) => void; label?: string }) { return <View accessibilityRole="tablist" accessibilityLabel={label} style={styles.control}>{options.map(option => { const selected = option.value === value; return <Pressable key={option.value} accessibilityRole="tab" accessibilityState={{ selected }} onPress={() => onChange(option.value)} style={({ pressed }) => [styles.option, selected && styles.selected, pressed && styles.pressed]}><Text style={[styles.label, selected && styles.selectedLabel]}>{option.label}</Text></Pressable>; })}</View>; }
const styles = StyleSheet.create({ control: { minHeight: theme.size.touch, padding: theme.space.xxs, borderRadius: theme.radius.sm, flexDirection: 'row', backgroundColor: theme.color.surfaceElevated }, option: { minHeight: 36, paddingHorizontal: theme.space.sm, borderRadius: theme.radius.xs, alignItems: 'center', justifyContent: 'center', flex: 1 }, selected: { backgroundColor: theme.color.accent }, label: textStyles.status, selectedLabel: { color: theme.color.white }, pressed: { opacity: 0.82 } });
